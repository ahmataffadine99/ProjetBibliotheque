{% extends 'base.html.twig' %}

{% block title %}Votre Profil{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow rounded-4 p-4" style="width: 100%; max-width: 450px;">
        <div class="text-center mb-4">
    <img src="https://via.placeholder.com/100/007bff/ffffff?text=👤" >

            <h2 class="mt-3">Votre Profil</h2>
        </div>

        <div class="mb-3">
            <label class="form-label fw-semibold">Nom :</label>
            <div class="form-control bg-light">{{ app.user.nom ?? 'Non renseigné' }}</div>
        </div>

        <div class="mb-3">
            <label class="form-label fw-semibold">Prénom :</label>
            <div class="form-control bg-light">{{ app.user.prenom ?? 'Non renseigné' }}</div>
        </div>

        <div class="mb-3">
            <label class="form-label fw-semibold">Email :</label>
            <div class="form-control bg-light">{{ app.user.email }}</div>
        </div>

        <div class="d-grid mt-4">
            <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Se déconnecter</a>
        </div>
    </div>
</div>
{% endblock %}
